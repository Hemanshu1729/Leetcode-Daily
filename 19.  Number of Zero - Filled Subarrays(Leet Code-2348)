class Solution:
    def zeroFilledSubarray(self, nums: List[int]) -> int:
        res,count=0,0

        for num in nums:
            if num==0:count+=1
            elif count!=0:
                res+=comb(count+1,2)
                count=0
        if count!=0:res+=comb(count+1,2)
        return res
