class Solution:
    def isPowerOfThree(self, n: int) -> bool:
        seen=False
        while n:
            if n%3==1:-
                if not seen:
                    seen=True
                else:return False
            elif n%3!=0:return False
            n//=3
        return seen
        
